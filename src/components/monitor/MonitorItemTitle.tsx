import "../../css/util/BoardUtil.css"

const MonitorItemTitle = ({title}: { title: string }) => {
        return (
            <div className={"item-title"}>{title}</div>
        )
}

export default MonitorItemTitle;